import { NavLink } from "react-router";

export default () => {
    return (
        <div>
            registration
            <NavLink to="/login">
                Login
            </NavLink>
        </div>
    )
}
